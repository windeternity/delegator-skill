# MOA Review Demo

This fixture shows two independent read-only reviewers inspecting the same
decision surface. It demonstrates `coordination_mode: moa_review` while keeping
the binding route as `FULL`.

Validate with:

```powershell
python -B scripts/validate-agent-inbox.py examples/moa-review-demo
```

